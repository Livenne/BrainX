spring:
  application:
    name: brainx-server

management:
  endpoints:
    web:
      exposure:
        include: health,info

server:
  port: 8080

brainx:
  state:
    sqlite-path: ${BRAINX_SERVER_DB:~/.brainx/server/brainx.sqlite}
